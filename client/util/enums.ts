export enum EditableJobPostSection {
    PostDetails,
    JobInfo,
    Forms,
    Publish
}

export enum ButtonSize {
    S,
    M,
    L
}

export enum ButtonStyle {
    Primary,
    Secondary,
    Tertiary
}
